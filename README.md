# autorun
Framework agnostic filewatcher

### Build

```bash
make tidy && make build
```
### Run

Point to the root of your `Go` project

```bash
./bin/autorun_v0.1.0.exe ~/dev/apps/multi-serve/
```

### TODO
- Add config support through `YAML`
- Figure out edge cases
